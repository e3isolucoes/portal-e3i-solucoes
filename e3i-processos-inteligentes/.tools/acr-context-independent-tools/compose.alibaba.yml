services:
  portal:
    build: .
    container_name: e3i-portal
    restart: unless-stopped
    env_file:
      - .env.production
    ports:
      - "80:3000"
    volumes:
      - e3i-data:/app/data

volumes:
  e3i-data:
