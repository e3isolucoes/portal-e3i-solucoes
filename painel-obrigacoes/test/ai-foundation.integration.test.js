import test from 'node:test'; import assert from 'node:assert/strict'; import { GeminiProvider } from '../src/ai/models/GeminiProvider.js';
test('GeminiProvider valida JSON estruturado e propaga usage real',async()=>{const provider=new GeminiProvider({apiKey:'not-real',fetchImpl:async()=>({ok:true,json:async()=>({candidates:[{content:{parts:[{text:'{"ok":true}'}]}}],usageMetadata:{promptTokenCount:3,candidatesTokenCount:2}})})}); const schema={safeParse:x=>x.ok?{success:true,data:x}:{success:false}}; const result=await provider.generate({model:'configured-model',systemInstruction:'rules',messages:[{role:'user',content:'data'}],outputSchema:schema,jsonSchema:{type:'object'},maxOutputTokens:10}); assert.equal(result.data.ok,true); assert.deepEqual(result.usage,{inputTokens:3,outputTokens:2,cachedTokens:null});});
