import { randomUUID } from 'node:crypto';
export class AITrace { constructor(sink={record:async()=>{}}) { this.sink=sink; } start(fields) { return {traceId:randomUUID(),...fields,startedAt:new Date().toISOString()}; } async finish(trace,result,error) { const completedAt=new Date(); const record={...trace,completedAt:completedAt.toISOString(),latencyMs:completedAt-new Date(trace.startedAt),inputTokens:result?.usage.inputTokens??null,outputTokens:result?.usage.outputTokens??null,cachedTokens:result?.usage.cachedTokens??null,status:error?'ERROR':'SUCCESS',errorType:error?.code??null}; await this.sink.record(record); return record; } }
